adapter driver ft232r
adapter speed 1000
ft232r_restore_serial 0x15

# lets you reset (with, e.g., `mon reset init`) if SRST (DCD on the
# FT232R) is connected to the RUN pin on the Pi. (it'll pull DCD low,
# which resets the Pi)
reset_config srst_only